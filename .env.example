# Jina Reader — free web search & scrape (https://jina.ai/reader)
# Optional but recommended for higher rate limits
JINA_API_KEY=

# OpenAI — structures raw crawled content into clean event cards
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
